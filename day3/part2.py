# import sys

# length = 12

# # since both have to be adde, it doesn't matter if i get the name correct
# def getCommonBit(data):
#     val = 0
#     with open(sys.path[0] + '/test.txt') as f:
#         for iter, line in enumerate(f):
#             val += int(line[n])





#         for i in range(0,length):
#             values[i] += int(line[i])
# print(values, iter)     
# for i in range(0, length):
#     gamma[i] = str(int(values[i] > (iter/2)))
#     epsilon[i] = str(int(values[i] < (iter/2)))
# gamma = "".join(gamma)
# epsilon = "".join(epsilon)
# print(f"gamma: {gamma}, eps:{epsilon}") 

# power_consumption = int(epsilon,2)*int(gamma,2)
# print(power_consumption)

